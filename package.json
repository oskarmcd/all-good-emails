{
  "name": "all-good-emails",
  "version": "0.0.1",
  "scripts": {
    "test": "jest --config jestconfig.json",
    "build": "tsc",
    "prepare" : "yarn build"
  },
  "description": "An email validator / regex that also considers the email TLD",
  "main": "index.ts",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.4",
    "typescript": "^4.2.3"
  }
}
